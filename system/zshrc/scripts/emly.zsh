# Source the emly shell-function toolkit so `emly <subcommand>` works in any shell.
# The toolkit is defined as a function (not a binary), so it must be sourced.
[[ -f "$HOME/Developer/emly/scripts/emly-tools.sh" ]] && source "$HOME/Developer/emly/scripts/emly-tools.sh"
