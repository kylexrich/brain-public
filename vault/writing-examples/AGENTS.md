> **`AGENTS.md` Instruction Precedence (DO NOT EDIT)**
>
> Hierarchical and additive — apply this file plus all parent `AGENTS.md` files up to repo root. Most specific wins on conflict.
>
> **Chain:** `vault/writing-examples/AGENTS.md` _(this file)_ > `vault/AGENTS.md` > `AGENTS.md` _(root)_

---

# `/vault/writing-examples` Writing Examples

Writing samples and drafts.

## Frontmatter Rules & Guidelines

All common frontmatter from `vault/AGENTS.md`.

| Field | Kind | Description |
|---|---|---|
| `status` | common | Default: `evergreen` (writing examples are already in a final state). |
| `last_reviewed_at` | common | Set to `created_at` on creation. |

## Content Rules & Guidelines

Freeform. No constraints — this folder is intentionally lightweight.
> Also note "### Linking Standard [STRICT]" in and other rules in `vault/AGENTS.md`.
